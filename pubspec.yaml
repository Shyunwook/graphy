name: graphy
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  quark:
    git:
      url: https://github.com/muabe/Quark.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.3.0

flutter:
